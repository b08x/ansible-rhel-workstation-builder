interface:
  display_name: "Ansible Rhel Workstation Builder"
  short_description: "Repo-specific patterns and workflows for ansible-rhel-workstation-builder"
  default_prompt: "Use the ansible-rhel-workstation-builder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true