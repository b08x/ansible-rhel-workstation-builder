---
# Fedora specific shell packages and configuration

packages__zsh:
  - zsh
  - zsh-syntax-highlighting
  - zsh-autosuggestions  # Available in standard repos

# Note: oh-my-zsh, zoxide, and zsh-autocomplete are not available in standard repos
# These are installed via tasks in the role
