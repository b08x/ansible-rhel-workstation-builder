---
# handlers file for docker role

- name: Restart Docker
  ansible.builtin.service:
    name: "{{ docker_service_name }}"
    state: restarted

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes