---
# vars file for sway on Rocky Linux 9/10
packages__sway:
  # Core Sway window manager and utilities
  - sway
  - swayidle
  - swaylock
  - swaybg
  - xdg-desktop-portal-wlr
  - xdg-desktop-portal-gnome
  - xwayland

  # GNOME / Desktop integration
  - gdm
  - gnome-keyring
  - polkit-gnome
  - NetworkManager-adwaita
  - blueman

  # Wayland utilities
  - grim # Screenshot tool
  - slurp # Region selector
  - wl-clipboard # Clipboard utilities
  - wf-recorder # Screen recorder

  # Application launchers and menus
  - fuzzel # Application launcher
  - wofi # Application launcher/menu
  - rofi-wayland # Rofi with Wayland support

  # Status bars and notifications
  - waybar # Status bar
  - mako # Notification daemon

  # Display and appearance
  - azote # Wallpaper manager
  - kanshi # Dynamic display configuration
  - wlogout # Logout menu

  # Terminal emulator
  - foot # Fast terminal emulator

  # System utilities required by scripts
  - jq # JSON processor
  - curl # HTTP client
  - bc # Calculator
  - brightnessctl # Brightness control
  - gettext # Translation tools (envsubst)
  - util-linux # System utilities (flock)
  - lsof # List open files
  - procps-ng # Process utilities (pgrep, pkill)
  - coreutils # Core utilities (date, mv, cp, etc.)
  - inotify-tools # File system event monitoring
  - libnotify # Desktop notifications
  - xdg-user-dirs # User directory utilities
  - systemd # System and service manager

  # Python dependencies for assign-cgroups.py and other scripts
  - python3
  - python3-requests # HTTP library for weather scripts
  - python3-dbus # D-Bus bindings
  - python3-i3ipc # i3/sway IPC library
  - python3-psutil # Process and system utilities
  - python3-tenacity # Retry library

  # Media control
  - playerctl # Media player control

  # Additional Wayland tools
  - wlsunset # Blue light filter

  # System monitoring and tools
  - gh # GitHub CLI
  - lm_sensors # Hardware monitoring

  # Desktop integration
  - desktop-backgrounds-compat # Default wallpapers

  # Fonts
  - google-roboto-fonts # Roboto font family
  - jetbrains-mono-fonts # JetBrains Mono font

# Directory definitions for user configuration
sway_directory_definitions:
  - dest: ".config/sway"
  - dest: ".config/waybar"
  - dest: ".config/mako"
  - dest: ".config/wlogout"
  - dest: ".config/sway/config.d"
  - dest: ".config/sway/inputs"
  - dest: ".config/sway/modes"
