
<script>

console.log("Current Date: " + new Date().toISOString());

</script>

**Generated Friday, January 30, 2026, represents a snapshot; system/code may evolve.**

**AI-Generated: Will likely contain errors or overlook nuances; treat this as one input into a human-reviewed development process**

# 📋 Consolidated Technical Backlog: Ansible RHEL Workstation Builder

The following backlog consolidates identified issues, risks, and suggested improvements from the System Design Review (Iterations 1 & 2). Tasks are prioritized by **Severity** (Critical impact on security/stability) and **Type** (Security, Refactoring, Reliability).

### 1. 🔴 Critical Security & Stability Tasks (Priority: P0)

|**Task ID**|**Task Name**|**Description & Acceptance Criteria**|**File Reference**|
|---|---|---|---|
|**SEC-01**|**Sanitize LLM Analyzer Payload**|**Issue**: The `llm_analyzer` plugin transmits raw task structures (`task._ds`), including sensitive parameters (passwords, keys), to external APIs.<br><br>  <br><br>**Action**: Implement a `sanitize_task_data` function in the python plugin to recursively scrub values for keys matching `password`, `secret`, `key`, `token` before serialization.<br><br>  <br><br>**Criteria**: No cleartext secrets appear in API payloads or generated logs.|llm_analyzer.py|
|**SEC-02**|**Externalize & Encrypt Secrets**|**Issue**: Default password hashes are hardcoded in role defaults, and the playbook lacks explicit Vault integration.<br><br>  <br><br>**Action**: Remove the `$6$rounds...` hash string from `roles/osbuild`. Ensure `playbooks/workstation.yml` loads `vars/secrets.yml` using `vars_files`.<br><br>  <br><br>**Criteria**: Secrets are only present in the Vault file; defaults will fail safely if variables are missing.|osbuild/defaults/main.yml|
|**REL-01**|**Fix Audio Tuning Idempotency**|**Issue**: The `tuned-adm profile` command reports "changed" on every run, breaking idempotency.<br><br>  <br><br>**Action**: Add a check task (e.g., `command: tuned-adm active`) to verify the current profile. Only execute the profile switch if the output differs.<br><br>  <br><br>**Criteria**: Second run of the playbook reports `ok=0 changed=0` for tuning tasks.|audio/tasks/tuning.yml|

### 2. 🟡 High Priority Refactoring & Architecture (Priority: P1)

|**Task ID**|**Task Name**|**Description & Acceptance Criteria**|**File Reference**|
|---|---|---|---|
|**REF-01**|**Abstract User Identity**|**Issue**: User `b08x` and UID `1000` are hardcoded in the playbook `vars`.<br><br>  <br><br>**Action**: Move the `user` dictionary to `inventory/group_vars/all.yml` or a `vars/users.yml` file. Refactor the playbook to loop through a `users` list if multi-user support is needed.<br><br>  <br><br>**Criteria**: Playbook can provision a user named `test_user` without modifying `workstation.yml` code.|playbooks/workstation.yml|
|**ARC-01**|**Implement Dynamic OS Versioning**|**Issue**: Hardcoded "Fedora 43" strings create a ticking clock for breakage upon release cycles.<br><br>  <br><br>**Action**: Replace static strings with `{{ ansible_distribution_major_version }}`. Add a `assert` task in `pre_tasks` to verify the host OS is supported.<br><br>  <br><br>**Criteria**: Playbook runs successfully on Fedora 40/41 without manual edits.|playbooks/workstation.yml|
|**REL-02**|**Native Module Adoption (Audio)**|**Issue**: Fragile `lineinfile` and `copy` tasks used for complex system configs.<br><br>  <br><br>**Action**: Refactor `sysctl` configurations to use `ansible.posix.sysctl`. Convert limits and udev rules to Jinja2 `templates`.<br><br>  <br><br>**Criteria**: Configuration files are generated deterministically; removing a key from vars removes it from the file.|audio/tasks/tuning.yml|

### 3. 🔵 Medium Priority Feature & Optimization (Priority: P2)

|**Task ID**|**Task Name**|**Description & Acceptance Criteria**|**File Reference**|
|---|---|---|---|
|**OPT-01**|**Local LLM Support**|**Optimization**: Reduce reliance on paid/public APIs for analysis.<br><br>  <br><br>**Action**: Update `llm_analyzer.py` to support an `ollama` provider pointing to `localhost:11434`.<br><br>  <br><br>**Criteria**: Analysis runs offline using a local model (e.g., Llama3).|llm_analyzer.py|
|**REF-02**|**Refactor Config Patching**|**Optimization**: Reduce configuration drift risks in `audio` and `base` roles.<br><br>  <br><br>**Action**: Identify all `lineinfile` tasks and replace with atomic `template` or `copy` tasks where complete file control is possible.<br><br>  <br><br>**Criteria**: Playbook execution is fully idempotent and verifiable.|[roles/audio/tasks](https://www.google.com/search?q=uploaded:b08x/ansible-rhel-workstation-builder/ansible-rhel-workstation-builder-4c144f1c5adaf7016b70a7ba57402dd638a64c65/roles/audio/tasks)|

### 📌 Implementation Strategy

1. **Immediate**: Execute **SEC-01** and **SEC-02** to secure the codebase before any further runs or commits.
    
2. **Short Term**: Address **REF-01** and **REL-01** to make the system portable and reliable for other developers.
    
3. **Mid Term**: Tackle **ARC-01** and **OPT-01** to improve long-term maintainability and developer experience.
